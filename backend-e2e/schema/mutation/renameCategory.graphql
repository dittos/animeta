extend type Mutation {
  renameCategory(input: RenameCategoryInput!): RenameCategoryResult!
}

input RenameCategoryInput {
  categoryId: ID!
  name: String!
}

type RenameCategoryResult {
  category: Category!
}
