extend type Mutation {
  updateRecordRating(input: UpdateRecordRatingInput!): UpdateRecordRatingResult!
}

input UpdateRecordRatingInput {
  recordId: ID!
  rating: Float # unset if null
}

type UpdateRecordRatingResult {
  record: Record!
}
