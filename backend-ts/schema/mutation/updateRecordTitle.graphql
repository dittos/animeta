extend type Mutation {
  updateRecordTitle(input: UpdateRecordTitleInput!): UpdateRecordTitleResult!
}

input UpdateRecordTitleInput {
  recordId: ID!
  title: String!
}

type UpdateRecordTitleResult {
  record: Record!
}
