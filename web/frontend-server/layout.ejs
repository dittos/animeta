<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title><%= title || '애니메타' %></title>
    <meta name="theme-color" content="#4bb000">
    <meta name="title" content="<%= title || '애니메타' %>">
    <meta name="description"
          content="<%= meta.description || '애니메타는 애니메이션 감상 기록장입니다. 어떤 작품을 몇 화까지 봤는지 감상평과 함께 기록하실 수 있어요.' %>">
    <meta name="keywords"
          content="<%= meta.keywords || '애니메타, 애니메이션, 만화, 드라마, 순위, 감상' %>">

    <meta property="og:site_name" content="animeta">
    <meta property="og:title" content="<%= title || '애니메타' %>">
    <meta property="og:type" content="<%= meta.og_type || 'website' %>">
    <meta property="og:url" content="https://animeta.net<%= meta.og_url || '/' %>">
    <meta property="og:description" content="<%= meta.description || '애니메타는 애니메이션 감상 기록장입니다. 어떤 작품을 몇 화까지 봤는지 감상평과 함께 기록하실 수 있어요.' %>">
    <meta property="og:image"
          content="<%= meta.og_image || (STATIC_URL + '/' + (meta.og_image_static || 'apple-touch-icon-180x.png')) %>">
    <meta property="fb:app_id" content="153413084690553">

    <meta name="twitter:card" content="<%= meta.tw_card_type || 'summary' %>">
    <meta name="twitter:url" content="https://animeta.net<%= meta.tw_url || '/' %>">
    <meta name="twitter:title" content="<%= title || '애니메타' %>">
    <meta name="twitter:description" content="<%= meta.description || '애니메타는 애니메이션 감상 기록장입니다. 어떤 작품을 몇 화까지 봤는지 감상평과 함께 기록하실 수 있어요.' %>">
    <meta name="twitter:image:src"
          content="<%= meta.tw_image || (STATIC_URL + '/' + (meta.tw_image_static || 'apple-touch-icon-180x.png')) %>">

    <link rel="apple-touch-icon-precomposed" sizes="120x120" href="<%= STATIC_URL %>/apple-touch-icon-120x.png"><!-- iPhone @2x -->
    <link rel="apple-touch-icon-precomposed" sizes="152x152" href="<%= STATIC_URL %>/apple-touch-icon-152x.png"><!-- iPad @2x -->
    <link rel="apple-touch-icon-precomposed" sizes="180x180" href="<%= STATIC_URL %>/apple-touch-icon-180x.png"><!-- iPhone @3x -->
    <link rel="icon" sizes="196x196" href="<%= STATIC_URL %>/touch-icon-192x.png"><!-- Android -->
    <link rel="shortcut icon" href="<%= STATIC_URL %>/favicon.ico">

    <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no, target-densitydpi=medium-dpi">
    <meta name="apple-mobile-web-app-capable" content="no">
    <meta name="google-site-verification" content="dhlhxHlQ_wDcoxu7hcyfWoXOyD79pPryal6fVLUxLeg">
<% assetEntries.forEach(entry => { %>
    <% if (assets[entry].css) { %>
    <link href="<%= ASSET_BASE %><%= assets[entry].css %>" rel="stylesheet">
    <% } %>
<% }) %>
<% if (!DEBUG) { %>
    <script>
      (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
      (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
      m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
      })(window,document,'script','https://www.google-analytics.com/analytics.js','ga');

      ga(function(){window.gaLoaded=true});
      ga('create', 'UA-7500828-2', 'auto');
      ga('send', 'pageview');
    </script>
<% } else { %>
    <script>function ga(){}</script>
    <script src="https://unpkg.com/mocha/mocha.js"></script>
    <link rel="stylesheet" href="https://unpkg.com/mocha/mocha.css" />
<% } %>
</head>
<body>
<div id="app"><%- content %></div>
<script>window.SENTRY_DSN = '<%= SENTRY_DSN %>'</script>
<% assetEntries.forEach(entry => { %>
    <% if (assets[entry].js) { %>
    <script type="text/javascript" src="<%= ASSET_BASE %><%= assets[entry].js %>"></script>
    <% } %>
<% }) %>
<script>window.__nuri && window.__nuri(<%- serializeJS(preloadData || {}, {isJSON: true}) %>);</script>
</body>
</html>
