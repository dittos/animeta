query UserRoute(
  $username: String!,
  $statusTypeFilter: StatusType,
  $categoryIdFilter: ID
) {
  currentUser {
    ...UserLayout_currentUser
  }
  user: userByName(name: $username) {
    ...UserLayout_user
    ...Library_user
  }
}
