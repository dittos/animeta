query UserLayout_commonPrivate {
  currentUser {
    ...UserLayout_currentUser
  }
  user: currentUser {
    ...UserLayout_user
  }
}

fragment UserLayout_currentUser on User {
  name
}

fragment UserLayout_user on User {
  id
  name
  joinedAt
  isCurrentUser
  recordCount
  postCount
}
